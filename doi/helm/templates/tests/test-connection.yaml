apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "doi.fullname" . }}-test-connection"
  labels:
    {{- include "doi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "doi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
