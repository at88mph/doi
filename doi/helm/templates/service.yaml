apiVersion: v1
kind: Service
metadata:
  name: {{ include "doi.fullname" . }}
  labels:
    {{- include "doi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "doi.selectorLabels" . | nindent 4 }}
